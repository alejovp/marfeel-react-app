import styled from 'styled-components';

export const StyledLogo = styled.a`
    text-decoration: none;
    font-weight: 600;
    cursor: pointer;
    position: relative;
    img {
        height: 36px;
    }
`;
